# Local development — copy to .env (never commit)
NODE_ENV=development

# Strict offline fixture mode is the default for foundation work.
DCE_FIXTURE_MODE=strict

# Database and provider credentials are added in later M1 tasks.
# DATABASE_URL=
# BETTER_AUTH_SECRET=
# PROVIDER_* keys belong in the worker environment only.
